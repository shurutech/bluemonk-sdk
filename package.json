{
  "name": "bluemonk-sdk",
  "version": "1.0.0-beta.1",
  "description": "BlueMonk Platform SDK",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "dist/"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "publishConfig": { "access": "public" },
  "keywords": [
    "bluemonk",
    "sdk",
    "api-client"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0"
  }
}
